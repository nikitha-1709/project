# Express Backend Lab

Run `npm install express cookie-parser express-session bcryptjs jsonwebtoken better-sqlite3` from the parent Node.js lab, then run any file with `node filename.js`. The examples use port 3000. REST endpoints can be tested with Postman or curl.